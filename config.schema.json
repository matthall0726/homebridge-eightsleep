{
  "pluginAlias": "EightSleep",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "Connect your Eight Sleep Pod to HomeKit for temperature control, bed presence detection, nap mode, and away mode.",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "default": "Eight Sleep",
        "required": true
      },
      "email": {
        "title": "Email",
        "type": "string",
        "required": true,
        "description": "Your Eight Sleep account email address."
      },
      "password": {
        "title": "Password",
        "type": "string",
        "required": true,
        "description": "Your Eight Sleep account password."
      },
      "pollingInterval": {
        "title": "Polling Interval (seconds)",
        "type": "integer",
        "default": 60,
        "minimum": 15,
        "maximum": 600,
        "description": "How often to poll the Eight Sleep API for status updates."
      }
    }
  }
}
